import { StyleSheet } from "react-native";
import { COLORS } from "../../constants/Theme";
const stylesReviews = StyleSheet.create({
    ReviewBorder:{
        paddingBottom:10,
        borderBottomWidth:0.5,
        borderColor:COLORS.lightgray,
    },

});
export default stylesReviews;