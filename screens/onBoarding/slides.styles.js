/*import { StyleSheet } from "react-native";
import { SIZES } from "../../components/constants/Theme.js";

const styles=StyleSheet.create({
    image :{
    resizeMode:"cover",
    width: SIZES.width,
    height:SIZES.height
},

    stack :{
        position :"absolute",
        bottom : 0,
        marginBottom : 60 ,
        marginHorizontal : 20 

    }
})
export default styles*/


/*import { StyleSheet } from "react-native";
import { SIZES, COLORS } from "../../components/constants/Theme.js";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  imageContainer: {
    ...StyleSheet.absoluteFillObject,
  },
  image: {
    resizeMode: "cover",
    width: SIZES.width,
    height: SIZES.height,
  },
  stack: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    marginBottom: 120,
    marginHorizontal: 20,
    padding: 15,
    backgroundColor: 'rgba(0,0,0,0.5)',
    borderRadius: 12,
  },
  paginationContainer: {
    position: 'absolute',
    bottom: 70,
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  paginationDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    marginHorizontal: 5,
  }
});

export default styles;*/

/*import { StyleSheet } from "react-native";
import { SIZES, COLORS } from "../../components/constants/Theme.js";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  image: {
    resizeMode: "cover",
    width: SIZES.width,
    height: SIZES.height,
  },
  stack: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    marginBottom: 60,
    marginHorizontal: 20,
    padding: 15,
    backgroundColor: 'rgba(0,0,0,0.5)',
    borderRadius: 12,
  },
  paginationContainer: {
    position: 'absolute',
    bottom: 30,
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  paginationDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    marginHorizontal: 5,
  }
});

export default styles;*/









/*import { StyleSheet } from "react-native";
import { SIZES, COLORS } from "../../components/constants/Theme.js";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  image: {
    resizeMode: "cover",
    width: SIZES.width,
    height: SIZES.height,
  },
  stack: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    marginBottom: 60,
    marginHorizontal: 20,
    padding: 15,
    backgroundColor: 'rgba(0,0,0,0.45)', // Slightly more transparent
    borderRadius: 12,
  },
  paginationContainer: {
    position: 'absolute',
    bottom: 30,
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  paginationDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    marginHorizontal: 5,
  }
});

export default styles;*/

/*import { StyleSheet } from "react-native";
import { SIZES, COLORS } from "../../components/constants/Theme.js";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  image: {
    resizeMode: "cover",
    width: SIZES.width,
    height: SIZES.height,
  },
  stack: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    marginBottom: 60,
    marginHorizontal: 20,
    padding: 15,
    backgroundColor: 'rgba(0,0,0,0.4)',
    borderRadius: 12,
  },
  paginationContainer: {
    position: 'absolute',
    bottom: 30,
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  paginationDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    marginHorizontal: 5,
  }
});

export default styles;*/


import { StyleSheet } from "react-native";
import { SIZES } from "../../components/constants/Theme";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  imageContainer: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    width: SIZES.width,
    height: SIZES.height,
  },
  image: {
    resizeMode: "cover",
    width: SIZES.width,
    height: SIZES.height,
  },
  stack: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    marginBottom: 60,
    marginHorizontal: 20,
    padding: 15,
    backgroundColor: 'rgba(0,0,0,0.4)',
    borderRadius: 12,
  },
  paginationContainer: {
    position: 'absolute',
    bottom: 30,
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  paginationDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    marginHorizontal: 5,
  }
});

export default styles;