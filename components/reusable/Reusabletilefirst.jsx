import { StyleSheet } from "react-native";

const ReusabletileFirst = StyleSheet.create({
   
    container: {
        flex: 1,
        marginHorizontal: 20,
      
      },

     rowWithSpace: (justifyContent) => ({
    
    alignItems: "center",
    justifyContent: justifyContent
  }),

  
});

export default ReusabletileFirst;